# —— Web（apps/web）——
# NEXT_PUBLIC_API_BASE=/api
# API_INTERNAL_BASE=http://127.0.0.1:8020
# CANONICAL_HOST=

# —— API（apps/api）——
# 元库（账号/设置等）：启动直连，勿在设置页配置
# SNS_META_DSN=postgresql://postgres:postgres@192.168.2.38:5439/nextweb
# SNS_CONFIG=    # 默认 <repo>/config/app.json
# SNS_COOKIE_SECURE=0
# 色花 / Bitmagnet 资源库 DSN：设置页或 config/app.local.json（resource_db / bitmagnet_db）

