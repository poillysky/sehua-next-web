/**
 * 精简 API 客户端 — 对接 apps/api（色花同协议），非整包复制 sehua 前端。
 *
 * 4,805 行 / 316 导出已按域拆分到 `lib/api/*`（见 lib/api/index.ts）。
 * 本文件保留为**重导出壳**，因此 `import { ... } from '@/lib/api'` 无需任何改动。
 */
export * from './api/index';
