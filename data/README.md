# 数据目录说明（运行时生成，勿提交大文件）
#
# app.sqlite       API 元库：用户 / 会话 / 设置
# lemon-session.json  柠檬过盾 Cookie（运行时，可删）
# library/         刮削导出产物（可选）
# maker-fs/        厂牌/前缀本地索引（可选）
# scrape_maps/     刮削映射缓存（可选）
# prefix-code-ranges.json  前缀范围缓存（可选）
